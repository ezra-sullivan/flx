# 2026-04-10 coordinator observe doc

- Added `doc/coordinator-observe.md` as the dedicated usage guide for `pipeline/coordinator` and `pipeline/observe`.
- Added `doc/examples/imgproc-observe.md` and `doc/examples/imgproc-coordinator.md` as the runnable docs for the split observe / coordinator examples.
- Updated `README.md`, `doc/guide.md`, and the imgproc example docs to point to the split imgproc example family: the main examples use Picsum list/download, while `imgproc_retry` remains local-only for deterministic retries.

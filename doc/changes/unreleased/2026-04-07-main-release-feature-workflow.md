# 2026-04-07 main-release-feature workflow

- Added `doc/git-workflow.md` to document the repository's `main-release-feature` branching model.
- Aligned `AGENTS.md` with the same branch, tag, and release rules so local collaboration guidance matches the public workflow doc.

# Release Notes

这个目录用于存放正式 release 的详细版本说明。

约定：

- 一个正式 release 对应一个文件
- 文件名使用版本号，例如 `v1.2.3.md`
- 根目录 `CHANGELOG.md` 只保留摘要历史
- 详细内容写在这里

如果某个 Git tag 不是正式 release，不要放到这里，改写入 `doc/tag-notes/`。

当前仓库还没有正式 release note 文件。
现有的 `v0.1.0`、`v0.1.1`、`v0.1.2` 都按 tag note 管理。
